json.extract! subscription, :id, :name, :phone_number, :email, :literary_habilitation, :address, :user_id, :classcourse_id, :description, :created_at, :updated_at
json.url subscription_url(subscription, format: :json)
